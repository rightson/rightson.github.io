# 文章發布規範

本規範適用於所有在此儲存庫新增、編輯及發布文章的 agent。
使用者當次明確指示優先於本文件。

## 1. 作者與署名

- 作者統一為 `Scott Yo-Ru Chen`。
- 每篇文章的 front matter 必須明確設定 `author`。
- 文章開頭的作者資訊由共用 layout 顯示，名稱使用較淡但清晰可讀的灰色。
- 作者名稱須連結至：
  https://www.linkedin.com/in/scott-yo-ru-chen-14711538/
- 正文不要重複加入「作者：……」，避免重複署名及污染首頁摘要。
- 必須確認正式文章頁實際顯示署名；僅設定 front matter 不代表完成。
- 作者列由共用 `_includes/post-byline.html` 統一產生，文章或 agent 不得自行複製一份 byline。
- 公開日期採自然語言顯示，例如 `2026 年 9 月 22 日`；不要在頁面顯示 `2026-09-22 17:35 +08:00` 這類機器格式，也不顯示時區。
- 在支援系統分享功能的手機／平板上，閱讀時間後顯示「分享到 Speechify」；點擊後以系統 Share Sheet 分享目前文章 URL，再由使用者選擇 Speechify。桌機不顯示此入口，也不跳轉 Speechify Web。此功能由共用 layout / JavaScript 提供，個別文章不得自行實作或移除。

## 2. 僅保留四大公開分類

每篇文章選擇一個主要 `domain`：

| domain | 公開分類 | 範圍 |
| --- | --- | --- |
| networking | 網路技術 | 網路協定、交換晶片、光通訊、AI 資料中心網路 |
| eda | EDA 設計平台 | SYN/APR、設計流程、EDA 自動化、驗證與設計平台 |
| ai-industry | AI 產業研究 | AI 需求、基礎設施、供應鏈、技術瓶頸與產業經濟 |
| investing | 投資與交易 | 投資假設、估值、催化劑、資金動向、交易與風險管理 |

- 依文章的核心問題分類，不因提及多種技術而增加分類。
- 光互連技術機制歸 networking；光通訊公司的估值分析歸 investing。
- 不新增第五種公開分類，不在首頁或文章頁展開細碎標籤。
- 新文章的 `categories` 使用與 `domain` 相同的單一值。
- 舊文章的 `categories` 可能構成網址，不可為重新分類而修改。
  舊文章應透過 `domain` 調整分類，保留既有網址。

## 3. 檔名與文章格式

路徑：
`_posts/YYYY-MM-DD-<english-kebab-slug>.md`

必要 front matter：

---
layout: post
title: "具體、有資訊量的標題"
date: YYYY-MM-DD HH:MM:SS +0800
author: Scott Yo-Ru Chen
domain: ai-industry
categories: ai-industry
description: "一至兩句概括文章的核心判斷，供首頁與搜尋摘要使用。"
---

- 將範例日期、分類及文字替換為實際內容。
- 日期採 Asia/Taipei 時區，避免意外設定未來日期而無法顯示。
- 標題由 layout 顯示，正文不重複放相同 H1。
- 正文以有內容的段落開始，使用有意義的 H2/H3 章節標題。
- 表格與程式碼必須能在手機上正常閱讀。

## 4. 寫作與研究品質

- 使用繁體中文，保留必要英文術語。
- 風格專業、精準、謙遜；以內容建立可信度，不自稱頂尖專家。
- 結論先行，說明問題的限制、作用機制、具體證據與後續影響。
- 不在文章中宣告寫作方法，例如「以下使用第一性原理分析」。
- 清楚區分來源事實、他人報告的實驗結果，以及作者推論。
- 關鍵事實附可追溯的原始來源連結，核對發布日期與事件日期。
- 不捏造實驗、績效、持倉、資歷、引言或確定性。
- 發文前檢查近期文章，避免在沒有新證據時重複相同論點。

對 SYN/APR 主題：
- 以具體 flow、Tcl/Perl、Makefile、logs、reports、
  RTL/netlist、SDC/UPF 及實作工具說明問題。
- 陌生的平台或 agent 技術須先定義，再以 SYN/APR 情境解釋。
- 避免只列抽象名詞，卻沒有實際工程機制與例子。

## 5. 保留既有網站與文章

- 一般發文任務不得順便更換 theme、style 或部署流程。
- 保留既有的四大分類、全文搜尋、文章目錄與手機閱讀功能。
- 未經要求，不改寫既有文章內容。
- 不任意修改既有檔名、日期、categories 或 permalink，
  以免破壞外部連結。
- 使用者已要求刪除以下文章，不得重新建立：
  - `_posts/2025-09-21-welcome-to-jekyll.markdown`
  - `_posts/2025-09-21-create-cline-cli.md`

## 6. 提交與發布

1. 發布前取得遠端預設分支最新狀態。
2. 保留其他 agent 同時提交的文章與修改；不得 force-push。
3. 檢查 front matter、作者、分類、摘要、來源與實際 diff。
4. 使用者或既有排程已授權直接發布時，提交至預設分支；
   否則遵循當次指定的 PR／審查流程。
5. 不將本地修改或本地 commit 說成「已發布」。
6. 不為催促部署反覆建立空 commit，也不擅自增加監控 workflow。

## 7. 驗證正式站

發布是否完成，須分別確認：

- 遠端 commit 已成功寫入。
- 對應 commit 的 GitHub Pages 建置成功。
- 部署成功。
- 正式文章 URL 顯示本次內容。

正式站檢查：
- 標題、正文及 LinkedIn 作者署名正確。
- 首頁、對應分類及 `search.json` 已包含新文章。
- 刪除文章後，確認相關列表與搜尋索引已移除。
- HTTP 200 不足以證明新版本上線，須核對頁面內容。
- 若修改 layout/style，須驗證正式站桌面與手機畫面，
  並確認 CSS 確實載入；不可只依本地預覽宣告修復。
- 樣式更新時使用版本化資源網址，避免新 HTML 套用舊快取 CSS。

## 8. 故障與完成回報

- 連線失敗、部署排隊或建置失敗時，明確說明目前狀態。
- 沒有證據，不推斷成使用者尚未認證或缺少權限。
- 保留已準備的修改，不得假稱遠端已更新。
- 完成回報保持精簡，提供文章連結及實際驗證結果。
- 尚未上線時，明確寫「尚未發布」並指出具體阻礙。
